# kubelet
This is a repo for our docker image of the kubelet binary.

This repo pulls down the upstream kubelet binary and adds additional packages needed for kubelet to work as a container image.

The repo follows the same LICENSE as upstream kubelet.
